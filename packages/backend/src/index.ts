/**
 * The library-tracker backend plugin.
 *
 * Add to a Backstage backend with:
 *   backend.add(import('backstage-plugin-library-tracker-backend'));
 */
export { libraryTrackerPlugin as default } from './plugin';
